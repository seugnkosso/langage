---------------creation de projet djangoRest api

------creer l'environnement virtuel
--py -m venv [nomVenv]

-----creer requirements.txt 
django>=4.0,<4.1
djangorestframework
pyyaml
requests
django-cors-headers

------installer les dépendance 
---pip install -r requirements.txt

-----mettre tous les dépendances installer dans requirements.txt
---pip freeze > requirements.txt

-----creer un dossier backend et frontend

-----creer un projet django
--django-admin startproject [nomProjet]

-----creer une application dans le projet 
--django-admin startapp [nomApp] 

---mettre l'application creer dans App



---------------------base api rest

------conversion des json en dictionnaire
--data = json.loads(jsonData)

------convertir de dictionnaire en json 
--pre_data = json.dumps(dic_Data)

-----recuperation du headers 
--data['headers'] = dic(request.headers)

-----récupèration des param d'Url
--data['params] = dic(request.GET)

----récupèration des parametre post 
--data['post_data'] = dic(request.POST)

---récupèration du contente_type
--data['content_type'] = request.content_type


----------------limit de django sur le reste